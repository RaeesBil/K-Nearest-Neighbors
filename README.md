# K-Nearest-Neighbors
K-Nearest Neighbors is a supervised learning algorithm used for classification and regression tasks. It works by finding the 'k' closest data points (neighbors) to a given input and assigning the most common class label (for classification) or averaging their values (for regression)
